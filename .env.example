# OpenRouter API Key (server-side only, NOT exposed to client)
# Get your API key from https://openrouter.ai/keys
# Required for AI deck generation feature (/generate route)
OPENROUTER_API_KEY=sk-or-v1-...your-key-here...

# Admin Password (temporary protection for /generate route)
# Set this to any secret password to gate access to AI generation
ADMIN_PASSWORD=something-secret-123

# Supabase Configuration (already configured)
# VITE_SUPABASE_URL=your-supabase-url
# VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
